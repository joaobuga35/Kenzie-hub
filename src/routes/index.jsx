import { Route, Routes, Navigate } from "react-router-dom";

export const RoutesMain = () => {
  return (
    <Routes>
      {/* <Route path="" element={}>
      </Route> */}
      {/* <Route path="*" element={} /> */}
    </Routes>
  );
};
